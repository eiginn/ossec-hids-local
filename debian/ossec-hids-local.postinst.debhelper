# Automatically added by dh_installinit
if [ -x "/etc/init.d/ossec-hids-local" ]; then
	update-rc.d ossec-hids-local defaults >/dev/null
#	invoke-rc.d ossec-hids-local start || exit $?
fi
# End automatically added section
